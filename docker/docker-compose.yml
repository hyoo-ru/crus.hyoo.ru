services:
  crus:
    build:
      context: .
      no_cache: true
    ports:
      - "9090:9090"
    logging:
        driver: "local"
    volumes:
      - dotcrus:/mam/hyoo/crus/app/run/-/.crus
      - state:/root/.local/share/mol_state_local
    restart: always

volumes:
  dotcrus:
  state:
