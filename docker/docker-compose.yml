version: '3'

services:
  crus:
    build:
      context: .
    ports:
      - "3000:3000"
    logging:
    	driver: "local"
    volumes:
      - dotcrus:/mam/hyoo/crus/app/run/-/.crus

volumes:
  dotcrus:
